name: Commit messages

on: pull_request

jobs:
  commitlint:
    runs-on: ubuntu-latest
    steps:
      # Without the full history there's no commit range to review.
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - uses: wagoid/commitlint-github-action@v6
